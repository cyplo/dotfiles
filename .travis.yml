language: 
    - bash

script: 
    - ./test 

env:
    - NOUPGRADE=true
    - DETECTED_OS=fedora:24
    - DETECTED_OS=debian:jessie
    - DETECTED_OS=ubuntu:trusty
    - DETECTED_OS=ubuntu:xenial

sudo: required

services:
      - docker
git:
    submodules: false

